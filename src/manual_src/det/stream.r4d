Stream texts to macro invocaiton

# This is technically a wrapper around relay macro

# Arguments

- a_macro_name : A macro target that following texts are relayed to ( trimmed )

# Example

$stream(squash)

abc

clo

fgh

$consume()
