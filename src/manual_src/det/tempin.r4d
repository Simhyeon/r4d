Include a temporary file

# Note

- A default temporary path is folloiwng
- Windows : It depends, but %APPDATA%\\Local\\Temp\\rad.txt can be one
- *nix    : /tmp/rad.txt
- This macro executes `include` macro for the temporary file

# NOT Deterred

# Example

```
$tempin()
```
