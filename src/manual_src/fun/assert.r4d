Compare lvalue and rvalue, panics with two values are not equal

# NOTE

Use assert flag to accumulate assertion results.

# Arguments

- a_lvalue : Left  value to compare
- a_rvalue : Right value to compare

# DEMO

```
$static(HO_VAR,$env(HO))

$other_macro|($HO_VAR())

$pipe(HO)
$assert(HOSE,$-())
===
# Ran with rad --assert --silent
assert fail:
 --> [INPUT = test]:2:2
error: found 1 errors

Assert
SUCCESS : 0
FAIL: 1
```

# Example

% Succeed
$assert(1,1)
% Fails
$assert(a,b)
