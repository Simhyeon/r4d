Split text into an array to get a value

# Arguments

- a_sep    : A separator string
- a_index  : An index to cut out
- a_text   : Text to split

# Demo

```
$syscmd|(curl wttr.in/Nuremberg?format=3)
$cut|-(:,1)
$-^()
===
☀️   +6°C
```

# Example

$assert(b,$cut(/,-2,a/b/c))
