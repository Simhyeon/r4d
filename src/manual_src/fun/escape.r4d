Escape processing for the input

# NOTE 
#
- This flow control only sustains for the current input.

# Demo

[1] First.r4d

```
$escape()
$decl(a)
$eval(1+1)
===
$decl(a)
$eval(1+1)
```

# Example

$escape()
