Get occurrences of an expression from source. This returns 0 if there are no
occurrences.

# Arguments

- a_expr   : An expression to match
- a_source : Source to match for

# Demo

```
$findm(m, There are multiple m's inside this expression)
===
2
```

# Example

$assert(2,$findm(o,hello world))
