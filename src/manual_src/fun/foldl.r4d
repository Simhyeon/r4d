Fold lines into a single value

# Note

- Foldl doesn't remove preceding spaces from each lines by default.

# Arguments

- a_lines : Lines to fold

# Demo

```
$foldl(
[
    a,
    b,
    c,
])
===
[    a,    b,    c,]
```

# Example

$assert(abc,$foldl(a
b

c))
