Check if left value is greater than or equal to right value

Consider using eval macro if you're trying mathematical evaluation.

# Return : Boolean

# Arguments

- a_lvalue : A left value to compare
- a_rvalue : A right value to compare

# Demo

```
$static(item_count,5)
$if($gte($item_count(),5),Yeah)
===
Yeah
```

# Example

$assert(true,$gte(c,b))
$assert(true,$gte(text,text))
