Check if a given value is empty

# Return : Boolean

# Arguments

- a_value : A value to qualify

# Demo

```
$isempty()
$isempty($space())
$if(
    $isempty($include(cache.txt)),
    $loge(Bad situation)
)
===
true
false
error: Bad situation
 --> [INPUT = test]:1:2 >> (MACRO = if):2:6
```

# Example

$assert(true,$isempty())
$assert(false,$isempty( )))
