Join an array into a single chunk with given separator

# Arguments

- a_item  : A separator used for joining
- a_array : Source to array to join

# Demo

```
$join(-,a,b,c)
===
a-b-c
```

# Example

$assert(a-b-c,$join(-,a,b,c))
