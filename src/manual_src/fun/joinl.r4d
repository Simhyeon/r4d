Join lines into a single chunk with given separator

# Note

- use foldl if you want to simply fold everything

# Arguments

- a_sep   : A separator used for joining
- a_array : Source to array to join

# Demo

```
$joinl(-,a
b
c)
===
a-b-c
```

# Example

$assert(a-b-c,$joinl(-,a,b,c))
