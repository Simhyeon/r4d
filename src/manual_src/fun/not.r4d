Returns a negated value of a given boolean. Yields error when a given value is 
not a boolean

# Note

- Use macro attribute "!" if you want simpler negation
- Use not macro when you can spare extra verbosity but need emphasis

# Arguments

- a_boolean : A boolean value to negate

# Demo

```
$not(true)
$not(1)
===
false
false
```

$not-()

# Example

$assert(false,$not(true))
$assert(true,$not(false))
$assert(false,$not(1))
$assert(true,$not(0))
