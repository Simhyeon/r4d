Strip expressions from rear

# Arguments

- a_expr   : Expressions to strip
- a_content : Content to strip from

# Demo

```
$stream(striper --)
local var = "wow" -- I'm comment
local var2 = "hmm_wow_doggy" -- I'm useless
$consume^()
===
local var = "wow" 
local var2 = "hmm_wow_doggy" 
```

# Test
