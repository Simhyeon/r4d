Strip from rear

# Arguments

- a_count   : Count of characters to strip
- a_content : Content to strip

# Demo

```
$stripr(2,Hello world with two newlines

)
===
Hello world with two newlines
```

# Test

$assert(Hmp,$stripr(2,Hmp::))
