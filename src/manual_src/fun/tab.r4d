Print tabs

# Note

- If environment variable RAD_TAB_WIDTH is set as unsigned
  integer, "tab" macro will print tab as corresponding spaces
  instead of literal "\t"

# Arguments

- a_amount : Amount of tabs to print [Unsigned integer] ( trimmed )

# Demo

```
% RAD_TAB_WIDTH=4
<-$tab()->
<-$tab(3)->
===
<-    ->
<-            ->
```

# Example

$assert($tab(2),		)
